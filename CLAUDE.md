# CLAUDE.md — Project Memory

> **READ THIS FILE AND `docs/PRICING_ENGINE_REQUIREMENTS.md` FIRST, EVERY SESSION, BEFORE ANY TASK.**
> The requirements doc is binding: vision, approach, architecture are locked. Do not simplify or substitute. If unclear — ASK, don't guess.
> **UPDATE the Decision Log below after every meaningful decision or change.**

## What this is
TSS Pricing Engine — an AI pricing workspace ("a pricing system that never existed"). Landed cost calculation from ERP costs + regional config, all pricing types, explainable traces, embeddable as a mashup in any CRM. Built for Trelleborg Sealing Solutions on SAP BTP; same codebase becomes a Pricing-as-a-Service SaaS later.

## Locked architecture (do not change without owner approval)
- Backend: CAP Node.js on SAP BTP Cloud Foundry (XSUAA, destinations to API6)
- Frontend: React (NOT Fiori/UI5) — mashup-portable
- Engine core: plain Node module, ZERO SAP dependencies (`engine-core/`)
- DB: HANA Cloud via CAP abstraction; must also run on Postgres (SaaS). NO HANA-specific SQL.
- All ERP/BI data via API6 only. Engine never integrates directly. API6 delivers raw facts, never pre-applied calculations.

## Non-negotiables (every PR)
- No hardcoded rates/factors/regional rules — everything numeric affecting a price is config
- No SAP imports in engine-core; no HANA SQL; no UI5
- Every FACTOR declares an explicit basis (engine refuses to run without it)
- Decimal math only (decimal.js), declared rounding; null ≠ 0; MISSING is a typed outcome, not an exception
- Deterministic kernel: no I/O inside engine-core; same input → same output
- Trace on every priced result; golden tests (`tests/golden/`) pass before merge
- Object-agnostic: engine prices items in context via neutral request; `purpose` (INDICATIVE|BINDING|REPRICE) drives behaviour, not host object type
- AI never writes a config directly: a natural-language instruction only ever produces a `PENDING_REVIEW` suggestion (config-model); applying one requires an explicit human `approvedBy` and re-validates against every non-negotiable first
- Cost access sequence (TSS's own, not SAP's condition technique) is config (`region-config.costAccessSequence`), never hardcoded — an ordered list of source systems (e.g. `["C4C","ERP","CCD","CCP"]`) engine-core walks to pick a cost candidate; explicit user selection still overrides it, and which system was actually used is always visible in the trace (`costCandidate.selectedBy`), never silent

## Repo map
```
engine-core/    pure pricing kernel (5 primitives: BASE, FACTOR, ADDER, PER_LINE, CONSTRAINT)
config-model/   config schemas, validation, versioning (effective-dated), scoped by (region, salesOrg)
srv/            CAP Node.js backend (pricing API, config API) — mocked auth locally, XSUAA in [production]
app/            React mashup UI — Direct (engine-core in-browser) and Backend API (CAP) modes
mcp-server/     MCP server exposing pricing tools
api6-client/    single door to API6 — pluggable outbound auth, recorded payloads in api6-client/recorded for dev
tests/golden/   verified real lines per region — must always pass
docs/           requirements doc + design docs
```

## Current phase
**Phase 1**: kernel + config model + golden tests, API6 client stubbed with recorded payloads. The original "one region end-to-end (India or Europe first)" framing has been deliberately superseded — the 2026-08-25 reference-doc review (see Decision Log, topics 1-4) walked through real TSS pricing logic for all four regions and found each one genuinely different (not a template to copy), so all four are now seeded with their real Appendix A landed-cost formulas: EUROPE, CHINA, INDIA, AMERICAS. Phase 2's "CAP API surface" has also landed early, at the owner's request (see Decision Log).
Status: `engine-core` kernel implemented and unit-tested — now with compound (`when`-array) conditional build-up elements and zero-fallback for skipped branches, on top of the original 5 primitives. `config-model` implemented: JSON-Schema-validated config tables, an in-memory versioned/effective-dated store, and the AI natural-language-instruction → suggestion → human-approval pipeline. `srv/` is a real CAP Node.js backend wiring engine-core + config-model + api6-client together end to end, with role-gated auth, and has deployment artifacts (`manifest.yml`, `docs/DEPLOYMENT.md`) for the owner to push to Cloud Foundry themselves. `app/` calls it (alongside its original in-browser mode). The actual UI "screens" are now being built separately in **SAP Build Apps** (a different app model — doesn't touch this repo), consuming `srv/`'s API once it's deployed. Still open: real finance-verified golden tests (the region configs are now real-formula-shaped but not yet signed off by finance as production rates), and actual persistence (config-model's store is in-memory, seeded fresh on every `srv/` boot — no HANA/Postgres yet, so it's also lost on every CF restage).

## Parked (owner decides — do NOT implement without instruction)
- AI-cost tracking (token/$ spend per suggestion) — the auditability *shape* (instruction, patch, rationale, confidence, model, reviewer) is now implemented in config-model; cost metering is not
- SaaS/IP ownership; governance model beyond a `PricingAdmin` role gate on config-changing endpoints (maker/checker, who *specifically* may approve what) — `srv/` now requires an authenticated PricingAdmin to approve/reject an AI suggestion, but there's still no distinct "maker" vs "checker" separation
- Real API6 / AI credentials — `api6-client` defaults to recorded payloads, `config-model`'s AI pipeline needs `ANTHROPIC_API_KEY` (unset today; endpoint reports `AI_NOT_CONFIGURED` rather than faking a response) — both explicitly deferred by the owner ("API key is for later")
- Real persistence (HANA Cloud / Postgres) — config-model's `ConfigStore` is in-memory only; `srv/` reseeds it on every boot
- Repo currently on personal GitHub; TSS git migration later (trivial: add remote, push)
- Config admin UI (browse/edit region configs, build-up elements, constraints, AI-suggestion review queue, supplier-config) — brainstormed with owner 2026-08-25, not built yet; `srv/`'s ConfigService already has the read endpoints, just no frontend
- Multiple pricing techniques beyond cost-plus/landed-cost (price list, variant, value-based, AI-proposed costs — the owner's confirmed list of 5) — brainstormed 2026-08-25, see Decision Log for the design sketch; not implemented

## Decision Log
- 2026-08-21: Requirements v1.0 finalized. Platform locked (CAP/CF + React + portable core). Object-agnostic model via `purpose` field. Repo scaffold created.
- 2026-08-25: Scaffold pushed to `AbdulNandalpad/PricingUnify` on GitHub (still personal, per parked TSS migration note). Concept deck (`TSS_Pricing_Engine_Concept.pptx`) added to `docs/` for reference alongside the binding requirements doc.
- 2026-08-25: `engine-core` implemented for real (kernel, cost resolution, 5 primitives, trace, decimal math, purpose gating) with unit tests (`node --test`, 6 passing) against a synthetic Europe-shaped build-up — not real TSS rates. `tests/golden/` still has no real finance-verified data, so `test:golden` remains a placeholder.
- 2026-08-25: Added a standalone dev UI in `app/` (Vite + React) at the owner's request, ahead of the Phase 3 mashup — it imports `@tss-pricing/engine-core` directly in the browser (no CAP/DB/API6 yet) so the kernel's build-up and trace are visible end-to-end. Uses the same synthetic sample data as the engine-core tests. `app` added to npm workspaces; `vite.config.js` sets `resolve.preserveSymlinks: true` + `optimizeDeps.include` so Vite's CJS→ESM interop works across the workspace symlink to engine-core. When `srv/` (CAP) exists, this UI should switch to calling the real pricing API instead of calling engine-core in-browser.
- 2026-08-25: `config-model` implemented at the owner's explicit instruction to unlock the previously-parked AI/instruction work early. Six tables: `region-config`, `build-up-element`, `constraint`, `resolution-rule`, `ai-suggestion`, plus a `provenance` fragment embedded on every one of them (source HUMAN|AI_SUGGESTED|AI_DERIVED|IMPORTED, confidence, rationale, approver) — every table is AI-ready from scratch, not just region-config. Added the AI pipeline: `suggestConfigChange()` (calls an AI client — real `@anthropic-ai/sdk` client or a `createFakeClient()` for hermetic tests — to turn a natural-language instruction into a JSON-Patch `AiSuggestion`, status `PENDING_REVIEW`) and `applySuggestion()` (requires a human `approvedBy`, re-validates against schema + the FACTOR-basis rule before creating a new version — throws and leaves the store untouched otherwise). No config is ever written by AI without an explicit human approval step. 19 unit tests pass, including one that prices an AI-approved config change through the real `engine-core` kernel. Also fixed a bug in the original scaffolded `region-config.schema.json`: it listed CONSTRAINT as a valid `buildUp` element type, but the kernel (built earlier this session) applies constraints from a separate `constraints[]` array after the build-up — schema now matches kernel behavior. `config-version-history` was considered as a seventh table but dropped: the store already keeps every version ever saved, so the version list *is* the history (see config-model/README.md).
- 2026-08-25: `config-model` extended to scope config documents by **(region, salesOrg)**, not region alone, per owner instruction — matches the object-agnostic request's `party: { customerId, salesOrg }` (requirements §7). `salesOrg: "*"` is a region-wide default; `region-config` and `ai-suggestion` both require `salesOrg` now. `ConfigStore` is keyed by `(region, salesOrg)`; `getEffectiveAsOf(region, salesOrg, date)` tries the sales-org-specific bucket first and falls back to the region's `"*"` bucket — a sales org only needs its own document where it actually diverges. Fixed a real bug this surfaced: superseding a version didn't close the old one's `validTo`, so two versions sharing a `validFrom` (e.g. an AI suggestion applied with no explicit new effective date) could both match the same lookup date, and `getEffectiveAsOf` would return whichever was inserted first rather than the actually-current one; `saveVersion` now closes the superseded version's window at the new version's `validFrom`. 24 unit tests pass (was 19).
- 2026-08-25: Backend + frontend orchestration wired end to end, at the owner's explicit request ("API key is for later. first lets create front end and backend orchestration with middleware connectors with different auth types") — pulling Phase 2's CAP API surface forward. Three pieces:
  - **`api6-client`**: pluggable outbound auth (`src/auth/strategies.js`) — `noAuth`, `apiKeyAuth`, `basicAuth`, `oauth2ClientCredentialsAuth` (caches/refreshes a bearer token), and `btpDestinationAuth` — a meta-strategy mirroring a real BTP Destination: it resolves credentials at call time via an injectable `resolveDestination(name)` and delegates to whichever of the other strategies the resolved destination declares. A generic `createConnector()` (recorded-payload replay in dev, real fetch + auth headers in live mode) sits under a concrete `Api6Client`. Defaults to recorded mode — no credentials needed until it's pointed at a real endpoint.
  - **`srv/`**: a real CAP Node.js backend (`@sap/cds`), not a stub. `PricingService.price` resolves the effective (region, salesOrg) config from config-model, facts from api6-client, and calls engine-core — unchanged pricing logic, now reachable over HTTP. `ConfigService` exposes read + AI-suggestion endpoints. Inbound auth is CAP's own profile-based system: `kind: "mocked"` locally with two demo users (alice/PricingViewer, bob/PricingAdmin, in the **root** `package.json`'s `cds` config — see gotcha below), `kind: "xsuaa"` under `[production]` (`xs-security.json` added, inert locally). `@requires: 'PricingAdmin'` now gates every config-changing endpoint — narrows (but doesn't fully close) the "any `approvedBy` string accepted" parked gap. The AI-suggestion endpoint checks for `ANTHROPIC_API_KEY` and returns `{status:'AI_NOT_CONFIGURED'}` rather than ever faking a response outside tests. No DB/CDS entities yet — config-model's in-memory `ConfigStore` is reseeded on every boot.
  - **`app/`**: a mode toggle — "Direct" (unchanged, engine-core in-browser) and "Backend API" (calls `srv/` with a demo-user picker, proxied via `vite.config.js`'s `server.proxy`). Both verified working in a real browser.
  - **CAP gotcha worth remembering**: CAP reads its `cds` config (and finds `srv/*.cds`) from `process.cwd()`, not from the entry script's directory. Because this is an npm-workspaces monorepo where `srv/` is its own package (with its own `package.json`), the `cds` config has to live in the **root** `package.json`, not `srv/package.json` — the latter is silently ignored, and CAP falls back to its own built-in sample mocked users (which happen to include an `alice` and a `bob` too, so a naive test can pass for the wrong reason — the actual bug this session hit was `bob` getting the *built-in* `cds.ExtensionDeveloper` role instead of our configured `PricingAdmin`). `srv/server.js` also force-`chdir`s to the repo root on boot so it behaves the same whether launched as `node srv/server.js` from the root or `npm start` from inside `srv/`.
  - Root `package.json` gained `"cds"` config and a `start:srv` script. 56 tests pass across all four workspaces (engine-core 6, config-model 24, api6-client 18, srv 8 — the last spawns the real server and hits it over HTTP).
- 2026-08-25: Owner synced this git repo into **SAP Build Apps** to build the actual UI "screens" there — a separate low-code app model, not a replacement for `app/` (confirmed with owner before proceeding, since CLAUDE.md locks the frontend to React/NOT Fiori/UI5, and "create screens" could have meant something that conflicted with that; it doesn't — Build Apps is a distinct consumer of `srv/`'s API). Owner also asked to deploy to CF; I have no `cf` CLI, BTP credentials, or VCAP env in this sandbox, so I can't run a deployment myself — owner will run it, I prepared the artifacts: `manifest.yml` (pushes the repo **root**, not `srv/` alone — `srv/` depends on the other workspace packages via npm workspaces symlinks, which only resolve if `npm install` runs at the root), `.cfignore`, and `docs/DEPLOYMENT.md` (full runbook: create the XSUAA service from `srv/xs-security.json`, push, get a token, connect Build Apps via a BTP Destination). None of this has been exercised against a real BTP org — flagged clearly in the runbook. Known rough edges for the owner to hit during their first deploy: production CORS is off (`[production]: {cors:false}`), and technical/client-credentials OAuth tokens (as opposed to a real logged-in user) won't automatically carry `PricingAdmin`/`PricingViewer` role-collection scopes unless the XSUAA client is explicitly granted them.
- 2026-08-25: `app/`'s "Backend API" mode rebuilt from a single-item form into a real batch pricing workspace, per owner feedback that the old UI was "very basic" for what should feel like "a real standalone pricing app." `BatchWorkspace` (new default tab, labeled "Pricing workspace"): an editable parts grid (add/remove rows, paste or upload a CSV of `partNumber, quantity, COO`), global controls (signed-in-as, region, sales org, purpose), one batch call to `/rest/pricing/price` with all rows as `items[]` (the backend already supported this — the UI just never sent more than one item), and a results table with per-line status/price plus a click-to-expand trace row reusing the existing `TraceTable`. The old single-item form survives as "Kernel demo" (renamed from the mode-toggle radios to header tabs) — still useful for seeing engine-core's mechanics on one hand-edited line, just no longer the default or the only view. Added 3 more parts to `api6-client/recorded/europe-default.json` (was just `P-10023`) so a batch demo has real variety out of the box, including one `FALLBACK`-confidence part to demonstrate BLOCKED-under-BINDING without hand-editing facts. Caught and fixed a real layout bug during Playwright QA: a 4-column field grid was sized off viewport width via a media query, not the actual (narrower) panel width, so a field overflowed outside the panel — fixed by dropping to the existing 2-column `.field-grid`.
- 2026-08-25: Brainstormed with owner: admin config UI, supplier-specific MOLV/condition records, and multiple pricing techniques (owner confirmed the "5 types": cost-plus/landed-cost, price list, variant, value-based, AI-proposed costs). Owner corrected my initial SAP-condition-technique framing of "access sequence" — for TSS it specifically means an ordered **cost-source** fallback chain: C4C manual entry → ERP → CCD → CCP, not a generic multi-dimensional condition-table lookup. Implemented that concrete piece: `region-config.costAccessSequence` (e.g. `["C4C","ERP","CCD","CCP"]`) is new optional config; `engine-core`'s `resolveCandidate()` walks it to pick among a part's cost candidates by `source.system`, still deferring to an explicit user selection first and to the old `default`-id behavior when no sequence is configured (fully backward compatible — existing tests needed zero changes). Which system actually won is never silent: `trace.costCandidate.selectedBy` now reads `ACCESS_SEQUENCE:<system>` (vs `USER`/`DEFAULT`), and `app/`'s trace detail renders it as prose ("via cost access sequence → CCD"). Renamed the recorded fixture's old `"S4"` system tag to `"ERP"` for vocabulary consistency, and added two demo parts (`P-50099`: only CCD/CCP candidates, demonstrates falling through C4C+ERP; `P-60150`: both ERP and C4C, demonstrates C4C winning) — verified end-to-end in a real browser. 64 tests pass (was 56: engine-core 12, config-model 26, api6-client 18, srv 8). Still open from this brainstorm, not yet built: the config admin UI, supplier-specific condition records (MOLV by supplier — a different mechanism, a looked-up threshold rather than a cost-source fallback), and the other 4 pricing techniques (see Parked).
- 2026-08-25: Supplier-specific landed-cost adders/constraints implemented — freight, duty, tariff, MOLV, MOQ all overridable per supplier, on top of whatever the cost access sequence resolved. New config-model table `supplier-config`, scoped (region, salesOrg, supplier) with the same wildcard-fallback/versioning pattern as region-config (refactored the shared bucket/version machinery in `store.js` into `VersionedBucketStore` so both share it, not copy-paste). `srv/pricing-service.js` resolves each item's effective supplier-config (if `item.supplier` is set) and merges it over whatever API6 already put in facts — engine-core needed almost no changes, since ADDER/CONSTRAINT already read generically from `facts.elements`. The one real kernel addition: a new `MIN_QTY` constraint kind (for MOQ) — deliberately informational only, same precedent as MROQ being routing input rather than a price adjustment (requirements §5.1): it never changes the price, just surfaces in `constraintPasses` so a BINDING caller can see it. Demo: supplier `ACME` (freight 18/duty 9.5/tariff 12/MOLV 300/MOQ 25) vs. the generic per-part API6 values; a new demo part `P-70200` and a `supplier` column in the batch UI make this directly testable. 75 tests pass (was 64: engine-core 15, config-model 32, api6-client 18, srv 10).
- 2026-08-25: Read every worksheet of the owner-supplied `Pricing_Tabular_4_2.xlsx` (14 sheets: China 20250812, China Process, China, US, AMER IND Process, AMER IND Use Case, TMS [empty], AERO [empty], S4 Pricing, MOLV and MOV table, Kit, test list China, Europe, India) and the full `Global_Pricing_One_Stop_Guide.docx` (real TSS business rules + worked examples), per owner instruction, in full, before building anything further. Not yet acted on — findings brought to owner for discussion first. Headline findings: (1) our `costAccessSequence` demo (C4C→ERP→CCD→CCP) doesn't literally match any single region's real fallback chain — China's real chain is JDE China→SAP Europe (2-step, OOD-driven), Americas' is a 4-tier Additional-Cost ladder (MROQ Landed Cost→Supplier Catalog Landed Cost→Current Landed Cost→Direct Ship Cost), India has none, Europe has none documented; (2) real per-region landed-cost formulas differ structurally by **stock class (MTS vs Non-MTS/MTO/MTC)**, which our config/kernel doesn't branch on at all today — Appendix A of the docx is a clean authoritative (region × stockClass) formula matrix; (3) direct source conflict: xlsx `Europe` sheet says markup is 4.3%, but the docx's §12.7.1 says 4.7% and explicitly claims to override every other source — needs an owner decision on which is authoritative; (4) MOLV's real mechanism disagrees even across the owner's own sheets — older China/Americas sheets describe adjusting **cost** (`purchase price = MOLV/qty`), but the newest sheet (China 20250812, dated Aug 2025, carrying an explicit correction comment attributed to "Abdul") says adjust **quantity** instead (`qty = MOLV/unit price`) — differs from our current CONSTRAINT/FLOOR (price-floor-at-fixed-qty) implementation either way; (5) SPU (Ship Pack Unit — order qty must round up to a multiple) is real and distinct from MOQ/MIN_QTY, not yet modeled; (6) OOD (Origin of Data) vs COO vs supplier-country is a real 3-way branch in China's logic, not just the COO `when`-condition we use today; (7) quantity-break/price-list cost tables (a literal step-priced-by-qty table, e.g. qty 10→18.49, 25→15.41...) appear in the data — a distinct pricing technique from cost-plus; (8) Kit/BOM header-cost-is-sum-of-components is real and unmodeled; (9) Americas' LCA Handling Fee real rate change (6.2%→6.7%, effective Jan'26) is a concrete real-world validation of config-model's effective-dated versioning design; (10) a quote-level Approval Total/Approval Margin aggregation (highest Total + lowest Margin% per product group) is a real requirement (FR-05) entirely outside today's per-line kernel scope. Full discussion of these with the owner is the immediate next step before any further engine-core/config-model changes.
- 2026-08-25: Topic 1 of the reference-doc review (stock class MTS vs Non-MTS) implemented, per owner decisions from discussion: (a) stock class is a `when`-condition on buildUp/constraint elements — engine-core's existing generic `when` mechanism (already used for `item.coo`) needed zero kernel changes to support `item.stockClass`, no new scoping dimension; (b) raw ERP code -> canonical MTS/NonMTS normalization happens in srv, not engine-core, keeping the kernel ERP-agnostic; (c) that mapping is config-driven (new optional `region-config.stockClassMap`, e.g. `{OMT:'NonMTS', MTS:'MTS', 'MTS-Z':'MTS', ...}`), not a hardcoded heuristic. New `srv/pricing-service.js:applyStockClassNormalization()` runs after `applySupplierOverrides`, mutating each item with either a resolved `item.stockClass` or an `item.stockClassError` if the region declares a `stockClassMap` but a part's raw code is missing or unrecognized — engine-core's kernel short-circuits any item carrying `stockClassError` straight to `MISSING(STOCK_CLASS_UNRESOLVED)` *before* cost resolution even runs, so an unclassified part can never silently skip stock-class-conditioned pricing elements. `trace.stockClass` now surfaces the resolved value on every result (PRICED or not) for audit. EUROPE's seeded config now declares a real `stockClassMap` (covering every raw code seen across the reference sheets: MTS/MTS-Z/MTS-2C -> MTS, OMT/SMT/CMT/MTO/MTC -> NonMTS) and all 7 existing demo parts got classification facts added to `api6-client/recorded/europe-default.json` — purely additive/informational for now, since Europe's real (region x stockClass) formulas are still pending (that's topic 4); no existing price changed. Added demo part `P-90400` with a deliberately unmapped raw code (`ZZZ`) to exercise the failure path end-to-end. 86 tests pass (was 75: engine-core 19, config-model 37, api6-client 18, srv 12). Next up per the agreed topic order: topic 2, real cost-access-sequence per region (China's real JDE-China-then-SAP-Europe 2-step vs. Americas' 4-tier MROQ/Catalog/Current/Direct-Ship ladder), replacing the current generic C4C/ERP/CCD/CCP demo chain.
- 2026-08-25: Topic 2 of the reference-doc review (real cost access sequence per region) resolved into a much smaller change than originally scoped, per owner correction: Americas' 4-tier ladder (MROQ Landed Cost -> Supplier Catalog Landed Cost -> Current Landed Cost -> Direct Ship Cost) is **not** something engine-core/srv needs to decide — API6 already resolves which tier applies and hands back the right cost before it ever reaches us, in the normal host-system-integrated flow. `costAccessSequence`/`resolveCandidate()` stay exactly as built (their job — letting a manual cost or a preferred system win — is unrelated and unchanged); `resolution-rule.schema.json` stays dormant, not wired up. The one real gap: a **standalone** flow, where a business user without host-system context wants to type a hypothetical MROQ and see the cost at that quantity break. Built as: (a) neutral request items gained optional `ood` and `warehouse` fields (material-master data, same pattern as `coo`/`supplier` — owner: "It is part of material master which api will pass... We have ood, warehouse, 2 fields that api6 needs to fetch cost correctly"); (b) new `srv/pricing-service.js:applyMroqOverrides()` — when `item.mroqOverride` is set **and** `item.ood === 'SMA'` (Americas' real OOD code per owner: Americas=SMA, China=CN, India=IN, Europe=SAP/SME), it looks up `facts.qtyBreaks[partNumber]` (a step-priced-by-quantity table — the shape a real parameterized API6 call would return) for the matching tier and adds it as one more cost candidate, explicitly selected via the existing `item.selectedCostId` "explicit selection always wins" precedence — no kernel changes needed. Demoed under EUROPE's existing seeded config (part `P-90500`, real China quantity-break numbers from the xlsx reused for convenience) rather than standing up a full Americas region, since Phase 1 is explicitly scoped to one region end-to-end. `app/`'s batch grid gained OOD and MROQ-override columns; fixed a layout bug this surfaced immediately (2 more columns squeezed every input unreadably narrow instead of scrolling — `.item-grid` had no horizontal-scroll container) by wrapping the table in a new `.item-grid-scroll` div. 89 tests pass (was 86: engine-core 19, config-model 37, api6-client 18, srv 15). Next up: topic 3, OOD vs COO vs supplier-country in China's real 3-way branch (`item.ood`/`item.coo` now both exist on the request shape as of this and earlier topics, ready to condition on).
- 2026-08-25: Topic 3 of the reference-doc review (OOD vs COO vs supplier-country, China's real 3-way cost-route branch) implemented — per owner correction, "OOD needs to be considered in the logic, and NOT the Supplier Country." Real China logic branches on OOD (JDE China vs not) AND supplier (is it "88058"/LCE) AND COO (US vs not) — three fields at once, which the existing single-comparison `when` grammar (`"path OP literal"`) couldn't express. Two small, general engine-core changes made this possible without any code-branch/hardcoded-rule (every non-negotiable stays intact): (1) `when` now also accepts an **array** of expressions, AND-ed together — `evaluateWhen` recurses on arrays, one line of kernel code; (2) a `when`-skipped build-up element now contributes a recorded **zero** to `ctx.stepValues` (and a "skipped" trace step) instead of being invisible — needed so a later FACTOR can safely list two mutually-exclusive COO-branch elements (e.g. `FREIGHT_DUTY_US`/`FREIGHT_DUTY_NONUS`) in its `basis` without crashing on whichever one didn't fire, and so a BINDING caller can see *why* a branch didn't apply, not just what happened in the branch that did. Confirmed this matches the binding requirements doc, not just an invented shape: §5.1 explicitly names "China F&D ×1.32/×1.21" as the canonical `COMPOSITE`/`allocatable:false` FACTOR example — the two freight&duty elements are tagged accordingly. New CHINA region-config (`srv/lib/seed.js`) — deliberately minimal (7 buildUp elements, no stock-class/MOLV/MOQ/pick yet, that's topic 4) — encodes the real 3-way branch: OOD="CN" → cost already landed, only 3.2% LCS markup; OOD≠"CN" + direct supplier (≠"88058") → freight&duty by COO (×1.32 US / ×1.21 non-US) then 3.2% markup; OOD≠"CN" + supplier="88058" (LCE) → same chain plus a further 6% LCE markup. `resolution` array documents China's real JDE-China→SAP-Europe fallback via the `resolution-rule` schema's `originOfData`/`fallback` fields (still dormant/unconsumed by engine-core, per topic 2's decision — documentation only). New `api6-client/recorded/china-default.json` with 5 demo parts (`CN-P001`..`CN-P005`), one per real branch, all sharing a flat 100.00 CNY base cost so the multiplier math is hand-verifiable. This is the first region-config beyond EUROPE — a deliberate, owner-approved expansion past Phase 1's original "one region end-to-end" framing, tracked here for transparency. 101 tests pass (was 89: engine-core 23, config-model 39, api6-client 18, srv 21). Verified end-to-end in a real browser — all 5 China routes price to the exact hand-computed values, and the trace visibly shows skipped branches (delta 0, no basis/source) for full audit. Next up: topic 4, real (region × stockClass) landed-cost formulas from Appendix A — reconciling this topic's cost-route branching with topic 1's stock-class dimension and topic 5's still-open MOLV mechanism question.
- 2026-08-25: Resolved the Europe markup conflict flagged in the initial reference-doc review (xlsx `Europe` sheet said 4.3%, docx §12.7.1 said 4.7% and claimed override authority over every other source) — owner confirmed 4.7% is authoritative. No code change needed: `srv/lib/seed.js`'s `SCM_MARKUP` was already `rate: 0.047`. Two items from the original findings list remain open: the MOLV mechanism conflict (quantity-adjustment vs cost-adjustment, topic 5) and topic 4 itself (full region × stockClass formula matrix from Appendix A, reconciling topic 1's stock-class dimension with topic 3's cost-route branching into one coherent real build-up per region).
- 2026-08-25: Topic 4 of the reference-doc review (full region × stockClass landed-cost formulas from Appendix A) implemented across all four regions — the biggest deliberate expansion past Phase 1's original "one region" framing yet, owner-approved. Resolved two more source conflicts first: India's overseas markup (xlsx "1.45%" vs docx Appendix A "+40%") — owner confirmed 40% is authoritative, treating the xlsx figure as a likely ×1.45-written-as-"1.45%" notation slip rather than a genuinely different rule; and confirmed "OOD is not the home region's own OOD code" (India: not "IN"; Americas: not "SMA") is what "overseas" means everywhere it appears, which let both regions' local/overseas split be a single plain `when` comparison — no new config field or srv normalization needed, simpler than originally planned. Also confirmed (before building China) that Appendix A's claim "MTS never gets freight&duty" is an oversimplification the detailed UC examples contradict — topic 3's China build-up was already correct as built; stock class turned out not to change China's build-up *structure* at all (only the BASE cost's source, which API6 already resolves per topic 2), so China only gained a `stockClassMap` for classification/audit visibility, no new elements. **Europe**: FREIGHT/DUTY now `when: stockClass === 'NonMTS'` (Appendix A: MTS = Moving Average + SCM + Pick only) — a real, deliberate price change for every MTS demo part (P-10023 was 113.8, now 106.8; documented in the test). **India** (new region): minimal build-up, just `BASE_COST` + a `when: ood !== 'IN'` +40% FACTOR — everything else (MOLV/freight/duty/pick) is genuinely "NA" per the India sheet. **Americas** (new region): Non-MTS gets FREIGHT/DUTY/TARIFF, both classes get the LCA Handling Fee (this region's name for SCM markup, local via `ood === 'SMA'` vs overseas) and a flat $34 Pick (literal `amount`, not per-part like Europe's `pickCharge` fact). Seeded as **two effective-dated versions** (2025.06.0 at the old 6.2%/10% rates, 2026.01.0 at the real Jan-2026-effective 6.7%/10.5% rates) — a concrete, real-world validation of config-model's effective-dating that CLAUDE.md has flagged as wanted since Phase 1 started; a test prices the same part as of a 2025 date and today, confirming both rates resolve correctly. 108 tests pass (was 101: engine-core 23, config-model 39, api6-client 18, srv 28). Verified end-to-end in a real browser across all four regions. Remaining open items from the original findings list: topic 5 (MOLV mechanism — quantity-adjustment vs cost-adjustment conflict), SPU, quantity-break/price-list pricing, Kit/BOM, quote-level Approval Total/Margin, and the S4-native Europe path / Additional-Cost flag scoping.
- 2026-08-25: Topic 5 of the reference-doc review (MOLV mechanism — quantity-adjustment vs cost-adjustment conflict) resolved as a config decision, per the owner: "give it as a config to the user to choose... it's a config decision" for both which mechanism applies *and* which regions use it (not universal — Americas' sheet still has the un-corrected "adjust cost" wording, with no correction comment). New optional `mode` field on `CONSTRAINT kind:FLOOR` (`config-model/schemas/constraint.schema.json`): `PRICE` (default, unset — the original behavior, recalculates unit price at the existing quantity) or `QUANTITY` (recalculates order quantity at the existing unit price, per the newest China sheet's stakeholder-corrected "Quantity = MOLV/Unit sell price"). `engine-core/src/elements.js`'s `applyConstraint` FLOOR branch returns a `quantity` field when in QUANTITY mode instead of adjusting `total`; `kernel.js`'s constraint loop threads a running `quantity` variable through the constraints array (so a later constraint, e.g. MOQ, sees the already-bumped quantity) and the final `result.quantity` reflects it. Deliberately scoped: QUANTITY mode does **not** re-run the build-up at the new quantity (PER_LINE elements like pick charge stay computed at the original quantity) — matches the sheets' literal "system will update qty" framing rather than a full reprice; flagged as a possible follow-up if the business needs true re-pricing-at-new-quantity semantics later. Wired into CHINA's seeded config only (`mode: 'QUANTITY'` on its MOLV constraint, new demo part `CN-P006`); Europe/Americas' supplier-config-driven MOLV stays on the default PRICE mode, unchanged. Caught and fixed a real UI gap this surfaced: the batch results table and constraint-passes list both silently showed the *unchanged* price and the *originally submitted* quantity, making a QUANTITY-mode adjustment invisible — `app/`'s results table now shows "5 (was 1)" when `result.quantity` differs from what was submitted, and the constraint-passes line renders `quantity 1 → 5` instead of the misleading `103.2 → 103.2` price fields when `mode === 'QUANTITY'`. 116 tests pass (was 108: engine-core 27, config-model 42, api6-client 18, srv 29). Verified end-to-end in a real browser. Remaining open items from the original findings list: SPU, quantity-break/price-list pricing, Kit/BOM, quote-level Approval Total/Margin, and the S4-native Europe path / Additional-Cost flag scoping.
- (append new entries here, newest last)
