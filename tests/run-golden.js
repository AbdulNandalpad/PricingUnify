/** Golden test runner: for each tests/golden/<region>/*.json
 *  { name, request, facts, configRef, expected: { unitPrice, trace summary } }
 *  Runs engine-core.price() and diffs. A failing golden test blocks merge. */
console.log('Golden runner — implement in Phase 1.');
