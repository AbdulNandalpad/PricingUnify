{
  "$id": "constraint.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Constraint",
  "description": "Floor/step adjustment applied after the build-up (MOLV, MOV, SPU rounding). MROQ is deliberately absent here — it is routing input that selects a scenario before pricing runs, not a constraint (see requirements §5.1).",
  "type": "object",
  "required": ["id", "type", "kind", "provenance"],
  "properties": {
    "id": { "type": "string", "pattern": "^[A-Za-z0-9_]+$" },
    "type": { "const": "CONSTRAINT" },
    "kind": { "enum": ["FLOOR", "STEP"] },
    "min": { "type": ["number", "string"], "description": "Literal floor value for kind=FLOOR (e.g. MOLV)." },
    "minRef": { "type": "string" },
    "step": { "type": ["number", "string"], "description": "Literal step size for kind=STEP." },
    "stepRef": { "type": "string" },
    "provenance": { "$ref": "provenance.schema.json" }
  },
  "additionalProperties": false
}
